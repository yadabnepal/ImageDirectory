import React from 'react';
import { Text, View, Image} from 'react-native';

const ImageBlock = (props) =>{
    return(
        <View>
            <Text>
                {props.title}
            </Text>
        </View>
    )
}